const inactiveColor = 'darkgrey';
const activeColor = 'navy';
const shadow = {
  shadowColor: '#000',
  shadowOffset: {
    width: 1,
    height: 1,
  },
  shadowOpacity: 0.4,
  shadowRadius: 2.22,
  elevation: 5,
};

export {inactiveColor, activeColor, shadow};
